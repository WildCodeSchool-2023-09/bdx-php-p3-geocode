{% extends 'base.html.twig' %}

{% block title %}Enregistrer son véhicule{% endblock %}

{% block body %}
<main>
    <div>
        <div>
            <h1 class="title">Mes voitures</h1>
        </div>
        {%for car in cars %}

        <div>
            <a href="{{ path("app_car_show", {'id': car.id}) }}">{{ car.brand }} - {{ car.model }}</a>
        </div>
        {% endfor %}

        <a href="{{ path('app_car_new') }}">+</a>
    </div>
</main>
{% endblock %}
